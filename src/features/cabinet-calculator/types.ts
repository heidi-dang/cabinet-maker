export type CabinetInput = {
    width: number;
    height: number;
    depth: number;
    thickness: number;
};

export type Panel = {
    name: string;
    width: number;
    height: number;
    quantity: number;
};
